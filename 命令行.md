ls /dev/tty*   
git commit -m ""
sudo chmod 666 /dev/ttyUSB0   // 修改设备节点的权限，让所有用户都能读写
sudo usermod -aG dialout $USER   // 将你的当前用户添加到 dialout 用户组中。这样你以后访问任何串口设备都不再需要 sudo 或修改权限。
stty -F /dev/ttyUSB0 -a   // 查看当前波特率
sudo stty -F /dev/ttyUSB0 460800    // 修改波特率

如果你不想每次都输入 sudo，可以修改系统的 limits.conf 文件，允许当前用户设置实时优先级。

    打开文件：
    code Bash

    sudo nano /etc/security/limits.conf

    在文件末尾添加以下两行（将 your_username 替换为你的实际用户名，或者用 @groupname）：
    code Text

    your_username    hard    rtprio          99
    your_username    soft    rtprio          99

    保存并重启系统（或者注销重新登录）使其生效。