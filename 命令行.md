ls /dev/tty*   
git commit -m ""
sudo chmod 666 /dev/ttyUSB0   // 修改设备节点的权限，让所有用户都能读写
sudo usermod -aG dialout $USER   // 将你的当前用户添加到 dialout 用户组中。这样你以后访问任何串口设备都不再需要 sudo 或修改权限。
stty -F /dev/ttyUSB0 -a   // 查看当前波特率
sudo stty -F /dev/ttyUSB0 460800    // 修改波特率