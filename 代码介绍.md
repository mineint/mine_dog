//支撑相
<!-- 
leg_controller：负责管理4条腿，协调所有腿的VMC计算、符号修正、安全限幅，并最终把力矩发送到CAN总线，让 LegController 只负责“执行命令”，而相位判断 + 选择控制方式放在上层。 
 -->
//摆动相
<!-- 
leg_swing：专门处理摆动相的轨迹生成 + 位置跟踪控制，最终输出关节力矩。
 -->
leg_kinematics：纯粹提供腿的几何/运动学计算，不涉及控制律、不计算力矩、不关心相位。
GaitScheduler：产生全局步态时钟，为每条腿分配本地相位，判断每条腿当前是 stance 还是 swing，支持不同步态的快速切换，提供统一的步态参数管理

这就非常明确了：你的步态生成器（Gait Generator）在处理非对称步长时，逻辑出现了偏移，导致某条腿的轨迹坐标超出了物理极限或计算范围。

既然“步长相同没问题”，说明你的逆运动学（IK）基础公式是对的。但当“步长不同”时，通常是以下三个深层逻辑出了问题：
1. 步幅中心点（Offset）的偏置问题 —— 最可能的原因

在直线行走（相同步长）时，腿的摆动范围通常是关于身体重心对称的。例如步长 10cm，轨迹是 -5cm 到 +5cm。

    问题所在： 当你把步长改为 15cm 时，如果你的算法没有重新对齐中心，它可能会变成从 -5cm 走到 +10cm（因为起始点没变，只增加了终点）。

    后果： 那个 +10cm 的位置可能已经达到了机械死角或超出了 IK 解算范围，于是系统报错，进入被动状态。

    检查： 检查长步长那条腿的坐标起始值和终点值。确保它是绕着“中性位置”对称摆动的（比如 -7.5cm 到 +7.5cm），而不是单向延伸。

2. 摆动相（Swing Phase）的复位错误

在差速转弯时，由于左右两侧线速度不同，如果你的代码是基于时间或者固定的步频来切换步态的：

    现象： 相同的摆动时间（Swing Time）内，长步长的腿需要更快的速度返回。

    逻辑漏洞： 很多简易算法在计算“回到起始点”的坐标时，使用了全局统一的变量。如果左腿步长 10，右腿步长 5，但程序在计算右腿“回弹”时误用了左腿的参数，右腿就会跳到一个错误的位置。

    检查： 检查每条腿的轨迹函数，确保其起始位置

            
    Xstart
    Xstart​

          

    和结束位置

            
    Xend
    Xend​

          

    是根据该腿自身的步长参数独立计算的。

3. 躯干扭转（Body Twist）补偿导致的干涉

很多四足机器人的算法在转弯时，会为了平衡或者更自然的姿态，给躯干加一个旋转补偿（Roll/Yaw）。

    现象： 当你设置差速时，程序可能自动触发了一个“转弯补偿”函数。

    后果： 这个函数会尝试旋转机身，这会给所有腿的坐标增加一个额外的偏移量。对于步长已经很大的那条腿来说，这个额外的旋转偏移量就是“最后一根稻草”，直接把足端坐标推向了不可达区域。

    实验： 尝试关闭所有躯干姿态补偿，只做单纯的差速（即机身水平，仅靠左右腿摆幅差行走），看是否还报错。

4. 步态相位失步（Phase Desync）

在差速时，长步长的腿由于物理移动距离长，如果电机响应跟不上轨迹规划的速度（例如加速度限制），它会产生滞后。

    现象： 到达步态切换点时，长步长的腿还没走到位。

    后果： 此时程序强制进入下一步，会导致坐标点的瞬时突变（跳变）。这种突变在空载测试时会表现为关节剧烈震动一下，然后触发驱动器的过流保护，进入被动模式。

调试建议：

    分步增加差异：
    不要直接设为一长一短。比如原本都是 5cm，尝试让左腿 5.1cm，右腿 4.9cm，观察串口输出的 IK 解算角度。

    打印关键变量：
    在代码中找出报错的那条腿，实时打印它的 Target_X 坐标。

        如果相同步长时，X 范围是 [-30, 30]；

        不同步长时，观察 X 是否变成了 [-30, 50] 这样不对称的范围。

    强制限制最大值：
    在进入 IK 函数前，对坐标做一个 constrain 限制。如果是因为坐标出界导致的，你会发现机器狗虽然不崩溃了，但长步长的那条腿会有明显的“截断感”。

本质结论： 你的程序在处理非对称步长时，没有正确地重新分布每条腿的运动区间，导致长步长的腿在触及运动终点时“越界”了。